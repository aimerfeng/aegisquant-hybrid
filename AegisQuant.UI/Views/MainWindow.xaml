<Window x:Class="AegisQuant.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AegisQuant.UI.ViewModels"
        xmlns:converters="clr-namespace:AegisQuant.UI.Converters"
        xmlns:interop="clr-namespace:AegisQuant.Interop;assembly=AegisQuant.Interop"
        xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        mc:Ignorable="d"
        Title="AegisQuant - Quantitative Backtesting System"
        Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
        <converters:IsNegativeConverter x:Key="IsNegativeConverter"/>
        
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        
        <Style x:Key="MetricLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="Gray"/>
        </Style>
        
        <Style x:Key="MetricValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <ToolBarTray Grid.Row="0" Background="#F5F5F5">
            <ToolBar Band="0" BandIndex="0">
                <Button Command="{Binding LoadDataCommand}" ToolTip="{DynamicResource LoadDataTooltip}">
                    <TextBlock Text="{DynamicResource LoadData}"/>
                </Button>
                <Separator/>
                <Button Click="LoadStrategyButton_Click" ToolTip="加载外部策略文件">
                    <TextBlock Text="加载策略" Foreground="Blue"/>
                </Button>
                <Button Click="NewStrategyButton_Click" ToolTip="创建新策略">
                    <TextBlock Text="新建策略" Foreground="Blue"/>
                </Button>
                <Separator/>
                <Button Command="{Binding StartBacktestCommand}" ToolTip="{DynamicResource StartTooltip}">
                    <TextBlock Text="{DynamicResource Start}" Foreground="Green"/>
                </Button>
                <Button Command="{Binding StopBacktestCommand}" ToolTip="{DynamicResource StopTooltip}">
                    <TextBlock Text="{DynamicResource Stop}" Foreground="Red"/>
                </Button>
                <Separator/>
                <Button Command="{Binding OpenOptimizationCommand}" ToolTip="{DynamicResource OptimizeTooltip}">
                    <TextBlock Text="{DynamicResource Optimize}"/>
                </Button>
                <Separator/>
                <Button Click="SettingsButton_Click" ToolTip="{DynamicResource SettingsTooltip}">
                    <TextBlock Text="{DynamicResource Settings}"/>
                </Button>
                <Separator/>
                <TextBlock Text="{DynamicResource Progress}" VerticalAlignment="Center" Margin="8,0,4,0"/>
                <ProgressBar Width="150" Height="18" Value="{Binding Progress}" Minimum="0" Maximum="100"/>
                <TextBlock Text="{Binding Progress, StringFormat={}{0:F1}%}" VerticalAlignment="Center" Margin="4,0,0,0"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - Parameters -->
            <Border Grid.Column="0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Margin="0,0,8,0" Padding="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource StrategyParameters}" Style="{StaticResource HeaderText}"/>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Label Grid.Row="0" Content="{DynamicResource ShortMaPeriod}"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ShortMaPeriod, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            
                            <Label Grid.Row="1" Content="{DynamicResource LongMaPeriod}"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LongMaPeriod, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            
                            <Label Grid.Row="2" Content="{DynamicResource PositionSize}"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PositionSize, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            
                            <Label Grid.Row="3" Content="{DynamicResource StopLossPct}"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding StopLossPct, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            
                            <Label Grid.Row="4" Content="{DynamicResource TakeProfitPct}"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding TakeProfitPct, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                        </Grid>
                        
                        <TextBlock Text="{DynamicResource RiskConfiguration}" Style="{StaticResource HeaderText}"/>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Label Grid.Row="0" Content="{DynamicResource MaxOrderRate}"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MaxOrderRate, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            
                            <Label Grid.Row="1" Content="{DynamicResource MaxPosition}"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MaxPositionSize, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            
                            <Label Grid.Row="2" Content="{DynamicResource MaxOrderValue}"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MaxOrderValue, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            
                            <Label Grid.Row="3" Content="{DynamicResource MaxDrawdownPct}"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding MaxDrawdownPct, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                        </Grid>
                        
                        <TextBlock Text="{DynamicResource DataInformation}" Style="{StaticResource HeaderText}"/>
                        <TextBlock Text="{Binding DataFilePath}" TextWrapping="Wrap" Foreground="Gray" FontSize="11" Margin="0,0,0,8"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Center Panel - Chart -->
            <Grid Grid.Column="1" Margin="0,0,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <scottplot:WpfPlot x:Name="EquityChart" Margin="4"/>
                        <TextBlock Text="{DynamicResource ChartPlaceholder}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="14" Visibility="{Binding IsDataLoaded, Converter={StaticResource InverseBoolToVisConverter}}"/>
                    </Grid>
                </Border>
                
                <!-- Metrics Bar -->
                <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Margin="0,8,0,0" Padding="16,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="{DynamicResource Balance}" Style="{StaticResource MetricLabel}"/>
                            <TextBlock Text="{Binding CurrentStatus.Balance, StringFormat=C2}" Style="{StaticResource MetricValue}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <TextBlock Text="{DynamicResource Equity}" Style="{StaticResource MetricLabel}"/>
                            <TextBlock Text="{Binding CurrentStatus.Equity, StringFormat=C2}" Style="{StaticResource MetricValue}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock Text="{DynamicResource TotalReturn}" Style="{StaticResource MetricLabel}"/>
                            <TextBlock Text="{Binding TotalReturnPct, StringFormat={}{0:F2}%}" Style="{StaticResource MetricValue}" Foreground="Green"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                            <TextBlock Text="{DynamicResource MaxDrawdown}" Style="{StaticResource MetricLabel}"/>
                            <TextBlock Text="{Binding MaxDrawdown, StringFormat={}{0:F2}%}" Style="{StaticResource MetricValue}" Foreground="OrangeRed"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="4" HorizontalAlignment="Center">
                            <TextBlock Text="{DynamicResource Positions}" Style="{StaticResource MetricLabel}"/>
                            <TextBlock Text="{Binding CurrentStatus.PositionCount}" Style="{StaticResource MetricValue}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Right Panel - Log -->
            <Border Grid.Column="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="{DynamicResource Log}" Style="{StaticResource HeaderText}" Margin="0,0,0,8"/>
                    
                    <ListView Grid.Row="1" ItemsSource="{Binding LogEntries}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="50"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="{Binding FormattedTime}" FontSize="10" Foreground="Gray"/>
                                    <TextBlock Grid.Column="1" Text="{Binding LevelString}" FontSize="10" FontWeight="Bold"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap" FontSize="11"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button Content="{DynamicResource Clear}" Command="{Binding ClearLogCommand}" Padding="8,4" Margin="0,0,4,0"/>
                        <Button Content="{DynamicResource Export}" Command="{Binding ExportLogCommand}" Padding="8,4"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="AegisQuant v1.0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
