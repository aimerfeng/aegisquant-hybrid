<Window x:Class="AegisQuant.UI.Views.ImportWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource String.Import.Title}"
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    
    <Window.Resources>
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SelectedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource DarkButton}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="MappingComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource String.Import.Title}" 
                       FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}"/>
            <TextBlock Text="{DynamicResource String.Import.Description}" 
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- File Selection -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="{DynamicResource String.Import.File}"
                       VerticalAlignment="Center" Margin="0,0,12,0"
                       Foreground="{DynamicResource PrimaryTextBrush}"/>
            <TextBox x:Name="FilePathTextBox" Grid.Column="1"
                     IsReadOnly="True"
                     Padding="8,6"
                     Background="{DynamicResource CardBrush}"
                     Foreground="{DynamicResource PrimaryTextBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     CaretBrush="{DynamicResource PrimaryTextBrush}"/>
            <Button Grid.Column="2" Content="{DynamicResource String.Browse}"
                    Style="{StaticResource DarkButton}"
                    Margin="8,0,0,0" Padding="12,6"
                    Click="BrowseButton_Click"/>
        </Grid>

        <!-- Preview Grid -->
        <Border Grid.Row="2" 
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" CornerRadius="4"
                Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{DynamicResource String.Import.Preview}"
                           FontWeight="SemiBold" Margin="12,8"
                           Foreground="{DynamicResource PrimaryTextBrush}"/>

                <!-- Data Preview Grid -->
                <DataGrid x:Name="PreviewDataGrid" Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="All"
                          Background="Transparent"
                          Foreground="{DynamicResource PrimaryTextBrush}"
                          BorderThickness="0"
                          Margin="8">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="Padding" Value="8,4"/>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>

                <!-- No Data Message -->
                <TextBlock x:Name="NoDataText" Grid.Row="1"
                           Text="{DynamicResource String.Import.SelectFileToPreview}"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Column Mapping -->
        <Border Grid.Row="3" 
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" CornerRadius="4"
                Margin="0,0,0,16" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{DynamicResource String.Import.ColumnMapping}"
                           FontWeight="SemiBold" Margin="0,0,0,12"
                           Foreground="{DynamicResource PrimaryTextBrush}"/>

                <ItemsControl x:Name="ColumnMappingPanel" Grid.Row="1">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="0,0,16,8">
                                <TextBlock Text="{Binding ColumnName}" 
                                           FontSize="11" Margin="0,0,0,4"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <ComboBox ItemsSource="{Binding MappingOptions}"
                                          SelectedItem="{Binding SelectedMapping, Mode=TwoWay}"
                                          Style="{StaticResource MappingComboBox}"
                                          SelectionChanged="MappingComboBox_SelectionChanged"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <!-- Date Format Selection -->
        <Border Grid.Row="4" 
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" CornerRadius="4"
                Margin="0,0,0,16" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{DynamicResource String.Import.DateFormat}"
                           VerticalAlignment="Center" Margin="0,0,12,0"
                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                
                <ComboBox x:Name="DateFormatComboBox" Grid.Column="1"
                          Style="{StaticResource MappingComboBox}"
                          MaxWidth="300" HorizontalAlignment="Left"
                          SelectionChanged="DateFormatComboBox_SelectionChanged">
                    <ComboBoxItem Content="Auto Detect" Tag="auto"/>
                    <ComboBoxItem Content="Unix Timestamp (seconds)" Tag="unix"/>
                    <ComboBoxItem Content="Unix Timestamp (milliseconds)" Tag="unix_ms"/>
                    <ComboBoxItem Content="yyyy-MM-dd" Tag="yyyy-MM-dd"/>
                    <ComboBoxItem Content="yyyy-MM-dd HH:mm:ss" Tag="yyyy-MM-dd HH:mm:ss"/>
                    <ComboBoxItem Content="yyyy/MM/dd" Tag="yyyy/MM/dd"/>
                    <ComboBoxItem Content="yyyy/MM/dd HH:mm:ss" Tag="yyyy/MM/dd HH:mm:ss"/>
                    <ComboBoxItem Content="MM/dd/yyyy" Tag="MM/dd/yyyy"/>
                    <ComboBoxItem Content="MM/dd/yyyy HH:mm:ss" Tag="MM/dd/yyyy HH:mm:ss"/>
                </ComboBox>

                <TextBlock x:Name="DetectedFormatText" Grid.Column="2"
                           VerticalAlignment="Center" Margin="12,0,0,0"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>
            </Grid>
        </Border>

        <!-- Progress and Buttons -->
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Progress Bar (hidden by default) -->
            <StackPanel x:Name="ProgressPanel" Grid.Column="0" 
                        Orientation="Horizontal" Visibility="Collapsed">
                <ProgressBar x:Name="ImportProgressBar" 
                             Width="200" Height="20"
                             IsIndeterminate="True"/>
                <TextBlock x:Name="ProgressText" 
                           Text="{DynamicResource String.Import.Importing}"
                           VerticalAlignment="Center" Margin="12,0,0,0"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>
            </StackPanel>

            <!-- Error Message -->
            <Border x:Name="ErrorBorder" Grid.Column="0"
                    Background="{DynamicResource ErrorBrush}" Opacity="0.2" 
                    BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                    CornerRadius="4" Padding="12,8" Visibility="Collapsed">
                <TextBlock x:Name="ErrorText" Foreground="{DynamicResource ErrorBrush}" TextWrapping="Wrap"/>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="{DynamicResource String.Cancel}" 
                        Style="{StaticResource DarkButton}"
                        Padding="20,8" Margin="0,0,8,0"
                        Click="CancelButton_Click"/>
                <Button x:Name="ImportButton" Content="{DynamicResource String.Import.Import}" 
                        Style="{StaticResource AccentButton}"
                        Padding="20,8" IsEnabled="False"
                        Click="ImportButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
