<UserControl x:Class="AegisQuant.UI.Controls.ReplayControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="800"
             Background="{DynamicResource SurfaceBrush}">
    
    <UserControl.Resources>
        <!-- Replay button style -->
        <Style x:Key="ReplayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="MinWidth" Value="36"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SelectedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledTextBrush}"/>
                                <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Row 1: Control Buttons and Speed -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Step Backward Button -->
                <Button x:Name="StepBackwardButton" 
                        Style="{StaticResource ReplayButtonStyle}"
                        ToolTip="{DynamicResource String.Replay.StepBackward}"
                        Click="StepBackwardButton_Click">
                    <TextBlock Text="⏮" FontSize="16"/>
                </Button>
                
                <!-- Play/Pause Button -->
                <Button x:Name="PlayPauseButton" 
                        Style="{StaticResource ReplayButtonStyle}"
                        ToolTip="{DynamicResource String.Replay.Play}"
                        Click="PlayPauseButton_Click"
                        MinWidth="48">
                    <TextBlock x:Name="PlayPauseIcon" Text="▶" FontSize="16"/>
                </Button>
                
                <!-- Step Forward Button -->
                <Button x:Name="StepForwardButton" 
                        Style="{StaticResource ReplayButtonStyle}"
                        ToolTip="{DynamicResource String.Replay.StepForward}"
                        Click="StepForwardButton_Click">
                    <TextBlock Text="⏭" FontSize="16"/>
                </Button>
                
                <!-- Reset Button -->
                <Button x:Name="ResetButton" 
                        Style="{StaticResource ReplayButtonStyle}"
                        ToolTip="{DynamicResource String.Replay.Reset}"
                        Click="ResetButton_Click"
                        Margin="8,2,2,2">
                    <TextBlock Text="⏹" FontSize="16"/>
                </Button>
                
                <!-- Separator -->
                <Rectangle Width="1" Fill="{DynamicResource BorderBrush}" Margin="12,4"/>
                
                <!-- Speed Control -->
                <TextBlock Text="{DynamicResource String.Replay.Speed}" 
                           VerticalAlignment="Center" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Margin="0,0,8,0"/>
                <ComboBox x:Name="SpeedComboBox" 
                          Width="80" 
                          SelectedIndex="2"
                          SelectionChanged="SpeedComboBox_SelectionChanged"
                          Background="{DynamicResource CardBrush}"
                          Foreground="{DynamicResource PrimaryTextBrush}"
                          BorderBrush="{DynamicResource BorderBrush}">
                    <ComboBoxItem Content="0.25x" Tag="2000"/>
                    <ComboBoxItem Content="0.5x" Tag="1000"/>
                    <ComboBoxItem Content="1x" Tag="500"/>
                    <ComboBoxItem Content="2x" Tag="250"/>
                    <ComboBoxItem Content="4x" Tag="125"/>
                    <ComboBoxItem Content="8x" Tag="62"/>
                </ComboBox>
                
                <!-- Separator -->
                <Rectangle Width="1" Fill="{DynamicResource BorderBrush}" Margin="12,4"/>
                
                <!-- Position Display -->
                <TextBlock Text="{DynamicResource String.Replay.Position}" 
                           VerticalAlignment="Center" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Margin="0,0,8,0"/>
                <TextBlock x:Name="PositionText" 
                           Text="0 / 0" 
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           FontWeight="SemiBold"
                           MinWidth="80"/>
                
                <!-- Separator -->
                <Rectangle Width="1" Fill="{DynamicResource BorderBrush}" Margin="12,4"/>
                
                <!-- Time Display -->
                <TextBlock Text="{DynamicResource String.Replay.Time}" 
                           VerticalAlignment="Center" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Margin="0,0,8,0"/>
                <TextBlock x:Name="TimeText" 
                           Text="--:--:--" 
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           FontFamily="Consolas"
                           MinWidth="140"/>
            </StackPanel>
            
            <!-- Row 2: Time Slider -->
            <Grid Grid.Row="1" Margin="0,8,0,0">
                <Slider x:Name="TimeSlider" 
                        Minimum="0" 
                        Maximum="100" 
                        Value="0"
                        IsSnapToTickEnabled="False"
                        IsMoveToPointEnabled="True"
                        PreviewMouseDown="TimeSlider_PreviewMouseDown"
                        PreviewMouseUp="TimeSlider_PreviewMouseUp"
                        ValueChanged="TimeSlider_ValueChanged"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
