<UserControl x:Class="AegisQuant.UI.Controls.EmergencyControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="280">
    
    <Border x:Name="MainBorder" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12"
            Background="{DynamicResource SurfaceBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 标题 -->
            <TextBlock Grid.Row="0" Text="紧急控制" FontWeight="Bold" FontSize="14" Margin="0,0,0,12"
                       Foreground="{DynamicResource PrimaryTextBrush}"/>
            
            <!-- 状态指示 -->
            <Border Grid.Row="1" x:Name="StatusBorder" CornerRadius="4" Padding="8,4" Margin="0,0,0,12"
                    Background="{DynamicResource CardBrush}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Ellipse x:Name="StatusIndicator" Width="12" Height="12" Fill="{DynamicResource SuccessBrush}" Margin="0,0,8,0"/>
                    <TextBlock x:Name="StatusText" Text="自动交易运行中" FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- 一键停止按钮 -->
            <Button Grid.Row="2" x:Name="EmergencyStopButton" 
                    Background="{DynamicResource DangerButtonBrush}" Foreground="White" 
                    FontWeight="Bold" FontSize="14"
                    Padding="16,12" Margin="0,0,0,8"
                    Click="EmergencyStopButton_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚠" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock Text="一键停止"/>
                </StackPanel>
            </Button>
            
            <!-- 一键清仓按钮 -->
            <Button Grid.Row="3" x:Name="CloseAllButton" 
                    Background="{DynamicResource WarningButtonBrush}" Foreground="White" 
                    FontWeight="Bold" FontSize="14"
                    Padding="16,12" Margin="0,0,0,8"
                    Click="CloseAllButton_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📤" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock Text="一键清仓"/>
                </StackPanel>
            </Button>
            
            <!-- 恢复按钮 (停止后显示) -->
            <Button Grid.Row="4" x:Name="ResumeButton" 
                    Background="{DynamicResource SuccessButtonBrush}" Foreground="White" 
                    FontWeight="Bold" FontSize="14"
                    Padding="16,12" Visibility="Collapsed"
                    Click="ResumeButton_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="▶" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock Text="恢复交易"/>
                </StackPanel>
            </Button>
        </Grid>
    </Border>
</UserControl>
